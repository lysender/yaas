syntax = "proto3";

package buffed.dto;

import "buffed/pagination.proto";

message UserBuf {
    string id = 1;
    string email = 2;
    string name = 3;
    string status = 4;
    int64 created_at = 5;
    int64 updated_at = 6;
}

message PaginatedUsersBuf {
    buffed.pagination.PaginatedMetaBuf meta = 1;
    repeated UserBuf data = 2;
}

message NewUserBuf {
    string email = 1;
    string name = 2;
}

message NewUserWithPasswordBuf {
    string email = 1;
    string name = 2;
    string password = 3;
}

message UpdateUserBuf {
    optional string name = 1;
    optional string status = 2;
}
